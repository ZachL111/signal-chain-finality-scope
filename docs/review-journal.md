# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its blockchain tooling focus without claiming live deployment or external usage.

## Cases

- `baseline`: `event finality`, score 198, lane `ship`
- `stress`: `nonce pressure`, score 217, lane `ship`
- `edge`: `settlement risk`, score 196, lane `ship`
- `recovery`: `proof depth`, score 177, lane `ship`
- `stale`: `event finality`, score 187, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
